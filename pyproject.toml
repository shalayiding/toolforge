[project]
name = "toolforge"
version = "0.1.0"
description = "Self-expanding AI toolbox — Claude finds, wraps, and registers GitHub repos as MCP tools"
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
    "chromadb",
    "httpx",
    "python-dotenv",
]

[tool.uv]
dev-dependencies = []
